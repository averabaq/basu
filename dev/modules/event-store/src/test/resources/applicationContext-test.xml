<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" 
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:tx="http://www.springframework.org/schema/tx" 
	xmlns:aop="http://www.springframework.org/schema/aop"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
                        http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
                        http://www.springframework.org/schema/tx
						http://www.springframework.org/schema/tx/spring-tx-3.0.xsd
                        http://www.springframework.org/schema/context
						http://www.springframework.org/schema/context/spring-context-3.0.xsd">

	<!-- enabling annotation driven configuration -->
	<context:annotation-config />
	<context:component-scan base-package="es.uc3m.softlab.cbi4pi.slave.event.store" />
	
	<tx:annotation-driven transaction-manager="transactionManager" />

	<bean class="org.springframework.orm.jpa.support.PersistenceAnnotationBeanPostProcessor" />

	<bean id="entityManagerFactory" class="org.springframework.orm.jpa.LocalContainerEntityManagerFactoryBean">
		<property name="persistenceXmlLocation" value="classpath:META-INF/event-store-persistence.xml" />
		<property name="dataSource" ref="dataSource"/>
		<property name="jpaProperties">
			<props>
				<prop key="hibernate.hbm2ddl.auto">${test.cbi4pi.slave.event.store.hibernate.persistence.mode}</prop>  
				<prop key="hibernate.dialect">${test.cbi4pi.slave.event.store.hibernate.dialect}</prop>  
				<prop key="hibernate.show_sql">${test.cbi4pi.slave.event.store.hibernate.show_sql}</prop>  
				<prop key="hibernate.archive.autodetection">${test.cbi4pi.slave.event.store.hibernate.archive.autodetection}</prop>
				
				<!-- It's set through the data source reference  
				<prop key="hibernate.connection.driver_class">${test.cbi4pi.slave.event.store.datasource.driver}</prop>  
				<prop key="hibernate.connection.username">${test.cbi4pi.slave.event.store.datasource.username}</prop>  
				<prop key="hibernate.connection.password">${test.cbi4pi.slave.event.store.datasource.password}</prop>  
				<prop key="hibernate.connection.url">${test.cbi4pi.slave.event.store.datasource.url}</prop>
				 -->  
			</props>
		</property>
		<property name="jpaVendorAdapter">
			<bean class="org.springframework.orm.jpa.vendor.HibernateJpaVendorAdapter">
			</bean>
		</property>		 
		 
		<!-- loadTimeWeaver -->
		<property name="loadTimeWeaver">
			<bean class="org.springframework.instrument.classloading.InstrumentationLoadTimeWeaver" />
		</property>
	</bean>

	<bean id="transactionManager" class="org.springframework.orm.jpa.JpaTransactionManager">
		<property name="entityManagerFactory" ref="entityManagerFactory" />
	</bean>
 
	<bean id="dataSource" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
		<property name="driverClassName" value="${test.cbi4pi.slave.event.store.datasource.driver}"/>
		<property name="url" value="${test.cbi4pi.slave.event.store.datasource.url}"/>
		<property name="username" value="${test.cbi4pi.slave.event.store.datasource.username}"/>
		<property name="password" value="${test.cbi4pi.slave.event.store.datasource.password}"/>
	</bean>
</beans>